# server {
#         listen 443 ssl;
#         http2 on;
#         server_name auth.drrr-sy.top;

#         ssl_certificate /etc/nginx/certs/drrr-sy.top/fullchain.pem;
#         ssl_certificate_key /etc/nginx/certs/drrr-sy.top/privkey.pem;

#         ssl_protocols TLSv1.2 TLSv1.3;
#         ssl_ecdh_curve X25519:P-256:P-384;
#         ssl_conf_command Ciphersuites TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256:TLS_AES_128_GCM_SHA256;
#         ssl_prefer_server_ciphers off;

#         ssl_session_timeout 1d;
#         ssl_session_cache shared:SSL:10m;
#         ssl_session_tickets off;

#         add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
#         add_header X-Frame-Options "SAMEORIGIN" always;
#         add_header X-Content-Type-Options "nosniff" always;
#         add_header X-XSS-Protection "1; mode=block" always;

#         location / {
#             proxy_pass http://zitadel:8080;

#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

#             proxy_set_header X-Forwarded-Proto https;
#             proxy_set_header X-Forwarded-Host $host;

#             proxy_http_version 1.1;
#             proxy_set_header Upgrade $http_upgrade;
#             proxy_set_header Connection "upgrade";

#             proxy_read_timeout 300s;
#             proxy_buffering off;
#         }

#     access_log /var/log/nginx/auth.access.log;
#     error_log /var/log/nginx/auth.error.log;
# }